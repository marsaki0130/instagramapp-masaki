= form_with(model:@comment, url:article_comments_path(@comment.article), local:ture) do |f|
  %div
    = f.label :content, 'コメント'
  %div
    = f.text_area :content
  =f.submit '保存', class:'btn-primary'